/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // typedRoutes conflicts with many placeholder / marketing links — re-enable when routes exist
};

export default nextConfig;
